# `000-EXAMPLE-feature/` — ejemplo lleno (read-only)

Una feature **completa y de referencia**: "marcar ítems como favoritos y ver la lista de favoritos". Los 4 artefactos están terminados y en su estado final (`spec.md` y `contract.md` ya `done`/`shipped`), para que veas **cómo se ve cada uno lleno**.

- Leelos al lado de las plantillas vacías de `../TEMPLATE/` — misma estructura, uno en blanco y otro completo.
- Corré `/validate-specs` y vas a verlo **pasar** el gate determinístico.
- **No lo edites.** **Borrá esta carpeta** cuando ya entiendas el flujo.
